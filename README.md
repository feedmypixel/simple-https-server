# A simple nodeJs https server

## Setup

Create certs by running:
```$ ./setupSslCerts.sh```

Install dependencies:
```$ npm i```

Allow insecure local host on chrome:
```chrome://flags/#allow-insecure-localhost```

Start server:
```$ npm start```
